# Java 21 Feature Test

This integration test ensures that the `jtcop-maven-plugin` operates correctly with Java 21 features.

To execute the test, run the following command:

```bash
mvn clean integration-test -Dinvoker.test=java21 -DskipTests
```

